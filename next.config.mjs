/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['swiperjs.com'], // Add this line to allow images from 'swiperjs.com'
    },
  };
  
  export default nextConfig;
  
